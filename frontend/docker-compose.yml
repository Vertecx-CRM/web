services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertecx-frontend
    env_file:
      - .env.docker
    ports:
      - "3000:3000"
    restart: unless-stopped
